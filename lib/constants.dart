library kourim.constants;

/// The key name for indexedDB storage.
const indexedDB = 'indexeddb';

/// The key name for local storage
const localStorage = 'localstorage';

/// The key name for session storage
const sessionStorage = 'sessionstorage';

/// The `findall` query
const findAll = 'findall';

/// The `find` query
const find = 'find';

/// The `row` strategy
const row = 'row';

/// The `rows` strategy
const rows = 'rows';

/// The `column` strategy
const column = 'column';

/// The `table` strategy
const table = 'table';

/// The `none` strategy
const none = 'none';

/// The `GET` http method
const get = 'get';

/// The `POST` method
const post = 'post';

/// The `PUT` method
const put = 'put';

/// The `DELETE` method
const delete = 'delete';